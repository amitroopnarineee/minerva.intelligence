# Archived Work

All previous pages, components, and features preserved here for reference.
The live product is now a single-page briefing experience.

To restore anything, move it back from this folder.

## Contents
- `app-routes/` — All previous page routes (insights, workspace, people, etc.)
- `app-config/` — Previous CSS overrides, API routes, loading states
- `components/` — Previous shared components (chat, menubar, command palette, etc.)
- `components/home/` — Previous home page components (HomeContent, etc.)
- `lib-backup/` — Copy of lib data files
